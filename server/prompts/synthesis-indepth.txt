Write ONLY a JSON object with exactly one key: "claims" - an array of strings. Output nothing before or after the JSON object. No prose, no markdown, no code fences.

GOAL: write the IN-DEPTH elaboration for the answer below - concise supporting context that is directly relevant to the question and direct answer and useful to a clinician. Every displayed claim must be grounded in the numbered evidence supplied to you.

You will be given, appended after this instruction:
- the DIRECT ANSWER (already written - do not repeat it), and
- the patient chart plus any GATHERED KB / EVIDENCE.

Write 1-3 claims. Prioritise, in order:
1. Additional patient-chart facts that directly clarify the answer.
2. A trend or comparison only when the cited records collectively state every value and date used.
3. Guideline or general-medical context only when a supporting numbered KnowledgeReference record is present in the supplied evidence.

Each string in "claims":
- States ONE checkable idea.
- Includes [N] citations to the smallest source set that supports it.
- Uses multiple citations when a comparison or trend depends on multiple records.
- Is limited to what those cited sources actually say: the cited source set must support the entire displayed claim.

Evidence rules:
- Do not use a patient chart citation as support for outside medical knowledge, a guideline, a threshold, a risk interpretation, or a recommendation that the cited chart record does not state.
- If no supporting KnowledgeReference record is present, do not add outside clinical interpretation. Use only additional chart context that the cited patient records directly support.
- A source that supports one clause does not support a second uncited clause. Split the ideas or omit the unsupported clause.
- [N] is reserved for real numbered evidence records only. Never emit a literal "[N]" placeholder or invent an index.
- If a useful idea cannot be fully supported by numbered evidence, omit it.

Do not restate the direct answer, list unrelated chart facts, or add generic advice. Return {"claims": []} when no additional relevant, fully supported context exists.
